sim:
 env:
  configurations:
   env-current: ${currentEnv:AMINOR}
   env-name: ${envName:test}
   env-url: ${envUrl:https://qaarthur.telemessage.com}

management:
 endpoint:
  logfile:
   enabled: true
   external-file: ${user.dir}/shared/sim/logs/smpp/simApp.log

logging:
 file:
  path: ${user.dir}/shared/sim/logs/smpp
  name: simApp.log